# books.json is the committed source of truth for the public site — do NOT ignore it.

# Legacy CSV backups (kept locally for reference only; books.json is the source of truth)
CSV/
catalog.csv

# macOS
.DS_Store

# VS Code workspace/local settings
.vscode/

# Node dev tooling (scripts/migrate-covers.mjs) — sharp is installed locally only
scripts/node_modules/

# Local secrets (never committed) — scripts/hardcover-summary.mjs reads this when HARDCOVER_TOKEN is unset
.hardcover-token

# Generated summary-fetch logs (regenerated on each backfill run)
scripts/hardcover-summary-misses.txt
